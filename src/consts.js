import * as T from './types.js';

/**
 * @type {T.Config}
 */
const DEFAULT_CONFIG = {
    VERSION: '1.0.0',
    canvasElement: null,
    padding: 10,
    grid: {
        size: {
            height: 100,
            width: 100,
        }
    },
};

export  { DEFAULT_CONFIG };

